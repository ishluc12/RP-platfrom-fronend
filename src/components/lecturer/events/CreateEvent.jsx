import React from 'react';

const CreateEvent = () => {
    return (
        <div>
            <h2>Create Event</h2>
            {/* Form to create a new event */}
        </div>
    );
};

export default CreateEvent;
