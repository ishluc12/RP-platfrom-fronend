import React from 'react';

const CreatePoll = () => {
    return (
        <div>
            <h2>Create Poll</h2>
            {/* Form to create a new poll */}
        </div>
    );
};

export default CreatePoll;
