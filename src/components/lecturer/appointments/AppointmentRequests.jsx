import React from 'react';

const AppointmentRequests = () => {
    return (
        <div>
            <h2>Appointment Requests</h2>
            {/* List of appointment requests from students */}
        </div>
    );
};

export default AppointmentRequests;
