import React from 'react';

const PollCard = () => {
    return (
        <div>
            <h3>Poll Card</h3>
            {/* Display for a single poll */}
        </div>
    );
};

export default PollCard;
