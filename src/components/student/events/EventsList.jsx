import React from 'react';

const EventsList = () => {
    return (
        <div>
            <h2>Events List</h2>
            {/* List of events for students */}
        </div>
    );
};

export default EventsList;
