import React from 'react';

const AppointmentCard = () => {
    return (
        <div>
            <h3>Appointment Card</h3>
            {/* Display for a single appointment */}
        </div>
    );
};

export default AppointmentCard;
